const port:number = 8080 // this is the port for the backend select your port number for spring
export const API = `http://localhost:${port}/api`
export const OAUTH_API = `http://localhost:${port}/oauth2/authorization`